const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};



// this 

const squareNum = (num) => {
  return num * num;
};

// can be refactored to

const squareNum = num => num * num;
