// variables can be declared through

let variableName; // declared variable, no value

const variableName2 = 1; // declared constant

// they can also be declared in a single line, such as

let variable1 = a, variable2 = b, variable3 = c; 